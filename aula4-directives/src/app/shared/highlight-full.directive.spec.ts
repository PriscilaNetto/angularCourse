import { HighlightFullDirective } from './highlight-full.directive';

describe('HighlightFullDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightFullDirective();
    expect(directive).toBeTruthy();
  });
});

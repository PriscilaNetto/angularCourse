import { DirectiveNgelseDirective } from './directive-ngelse.directive';

describe('DirectiveNgelseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveNgelseDirective();
    expect(directive).toBeTruthy();
  });
});
